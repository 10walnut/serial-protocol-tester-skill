interface:
  display_name: "Serial Protocol Tester / 串口协议测试器"
  short_description: "Generate and test serial protocol scripts"
  default_prompt: "Use $serial-protocol-tester to convert my serial protocol into a validated script and test it in the simulator."
