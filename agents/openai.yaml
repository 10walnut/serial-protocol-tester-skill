interface:
  display_name: "串口协议助手 / Serial Protocol Assistant"
  short_description: "Turn vendor serial docs into a test console"
  default_prompt: "Use $serial-protocol-assistant to convert my vendor serial protocol into a validated script, load it as a functional test console, and help isolate host, device, script, or link faults."
